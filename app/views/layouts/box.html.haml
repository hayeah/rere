- content_for(:toolbar) do
  - unless yield(:toolbar)
    = render(:partial => "shared/toolbar")
- content_for(:content) do
  .box
    .top
    .middle
      = yield
    .bottom

= render :file => 'layouts/application'